# 中文ESCO技能提取器依赖包

# 核心依赖
sentence-transformers>=2.2.0
pandas>=2.0.0
numpy>=1.24.0
torch>=2.0.0

# 中文Sentence Transformer模型推荐：
# 1. shibing624/text2vec-base-chinese (推荐，专门针对中文优化)
# 2. paraphrase-multilingual-MiniLM-L12-v2 (多语言支持)
# 3. moka-ai/m3e-base (中文embedding模型)

# 翻译API依赖（可选，根据需要安装）
# googletrans==4.0.0rc1  # Google Translate
# deepl>=1.15.0          # DeepL API
# openai>=1.0.0          # OpenAI API

# 数据处理
scikit-learn>=1.3.0
tqdm>=4.65.0

# 可视化（可选）
matplotlib>=3.7.0
seaborn>=0.12.0
